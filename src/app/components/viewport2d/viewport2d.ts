// viewport2d.ts
import { Component, OnDestroy, Input, effect, inject } from '@angular/core';

@Component({
  selector: 'app-viewport2d',
  standalone: true,
  imports: [],
  templateUrl: './viewport2d.html'
})
export class Viewport2D {
  
}